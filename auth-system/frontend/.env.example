# Development (Vite proxy handles /api → localhost:5000, so leave empty)
VITE_API_BASE_URL=

# Production (set this to your deployed backend URL, no trailing slash)
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
